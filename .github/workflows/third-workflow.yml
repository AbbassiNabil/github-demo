name: Third Workflow
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v6
      - name: Install Node
        uses: actions/setup-node@v6
      - name: Get dependencies
        run: npm ci
      - name: Build the app
        run: npm run build
